<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.hvilledev.www.forsightfood" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ca.hvilledev.www.forsightfood.EditUnit"></activity>
        <activity android:name=".Stores" ></activity>
        <activity android:name=".Manage_Items"></activity>
        <activity android:name=".Weights_and_Measures"></activity>
        <activity android:name=".Manage_Units"></activity>
    </application>

</manifest>
